# 任务：根据产品决策回复修正需求文档

## 背景

产品经理已对 `osg-spec-docs/docs/01-product/prd/admin/DECISIONS.md` 中的 6 个待决策项全部做出回复（回复内容在下方）。
你需要：
1. 更新 `DECISIONS.md` 的决策状态和结果
2. 将决策结果同步到对应的 4 个需求文档中，消除所有 `[DECISION NEEDED]` 标记

## 产品决策回复（来自 副本DECISIONS.docx）

### D-001：首次登录强制改密
**决策：B - 强制改密** — 首次登录成功后弹出改密弹窗，必须改完才能进入系统

### D-002：角色选择方式
**决策：B - 统一多选** — 新增和编辑都用 Checkbox 多选，一人可兼多角色（如文员 + 报销审核员）

### D-003：题库管理员是否拆分
**决策：B - 拆分** — 拆为「在线题库管理员」+「面试题库管理员」，精细控制

### D-004：角色标识命名风格
**决策：B - snake_case** — 如 job_admin, file_admin，后端/数据库标准

### D-005：文员权限范围
**决策：全部 Y** — 导师排期管理、学员求职总览、模拟应聘管理、课程记录、人际关系沟通记录、邮件、消息管理、投诉建议 全部授权给文员
**备注：权限可由超级管理员手工配置**

### D-006：权限模块列表补全
**决策：全部补入** — 学生自添岗位、学员求职总览、模拟应聘管理、人际关系沟通记录、课程记录、课时结算、在线测试题库、真人面试题库 全部加入权限模块列表
**备注：新增功能后自动补入**

## 需要修改的文件和具体修改点

### 文件 1：`osg-spec-docs/docs/01-product/prd/admin/DECISIONS.md`
- 将决策总览表格中所有「待确认」改为「已确认」
- 每个决策项的「决策结果」填入对应选项
- D-005 的表格填入 Y
- D-006 的表格填入「是」
- 确认记录表填入日期 2026-02-10

### 文件 2：`osg-spec-docs/docs/01-product/prd/admin/01-login.md`
- **第 5 节**：移除 `[DECISION NEEDED]` 标记，明确写入「需要开发首次登录强制改密功能」，补充强制改密弹窗的 UI 说明（参考找回密码 Step 3 样式）
- **第 6 节角色列表**：将 kebab-case 角色值改为 snake_case（super-admin → super_admin 等），题库管理员拆分为 online_quiz_admin 和 interview_admin

### 文件 3：`osg-spec-docs/docs/01-product/prd/admin/02-permission-roles.md`
- **第 2.2 节预置角色**：将「题库管理员」拆分为「在线题库管理员」和「面试题库管理员」两行
- **第 3.2 节权限模块结构**：补入 D-006 确认的 8 个缺失页面到对应分组
- **第 6 节**：移除 `[PROTOTYPE BUG]` 标记，改为已解决说明

### 文件 4：`osg-spec-docs/docs/01-product/prd/admin/03-permission-admins.md`
- **第 4.2 节**：移除 `[DECISION NEEDED]`，明确写入「统一为 Checkbox 多选」
- **第 4.3 节角色选项列表**：将「题库管理员」拆分为「在线题库管理员」和「面试题库管理员」，移除该处 `[DECISION NEEDED]`
- **第 5.1 节编辑弹窗**：将角色字段从 `select（单选）` 改为 `checkbox 组（多选）`

### 文件 5：`osg-spec-docs/docs/01-product/prd/admin/04-rbac-matrix.md`
- **第 4 节权限矩阵**：
  - 所有 `?` 改为 `Y`（文员权限全部确认）
  - 将 `qbank_admin` 列拆分为 `quiz_admin` 和 `interview_admin` 两列
  - quiz_admin 权限：首页、在线测试题库
  - interview_admin 权限：首页、真人面试题库、面试真题
- **第 5 节**：移除 `[DECISION NEEDED]`，确认使用 snake_case，题库管理员已拆分

## 约束
- 只修改 `osg-spec-docs/docs/01-product/prd/admin/` 下的 5 个 .md 文件
- 不要修改任何 Java 代码或前端代码
- 保持 Markdown 格式规范
- 使用中文输出
- 修改完成后在 osg-spec-docs 目录下执行 git add + git commit
