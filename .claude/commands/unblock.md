# /unblock 命令

## 用法

```
/unblock T-xxx            # 解除指定 Ticket 的阻塞
/unblock T-xxx "已确认使用 sys_role 表"  # 带说明的解除
/unblock --list           # 列出所有阻塞的 Tickets
/unblock T-xxx --cascade  # 级联解除依赖阻塞
```

## 说明

解除 Ticket 的 blocked 状态，使其可以继续执行。

## 执行流程

```
1. 检查 Ticket 状态（必须是 blocked）
2. 更新状态为 pending
3. 如果 --cascade，解除依赖的 Tickets
4. 更新 STATE.yaml
5. 输出解除报告
```

## /unblock --list

列出所有阻塞的 Tickets 及其原因。

## 输出示例

### 解除阻塞

```markdown
## ✅ 阻塞已解除

**Ticket**: T-003
**原阻塞原因**: 依赖外部 API 未就绪
**解除说明**: API 已就绪

### 状态更新
T-003: blocked → pending

### 级联解除
- T-004: blocked → pending
- T-005: blocked → pending

### ⏭️ 下一步
执行 `/next` 继续执行 T-003
```

### 列出阻塞

```markdown
## 🔴 阻塞的 Tickets

| ID | 标题 | 阻塞原因 | 阻塞时间 |
|----|------|----------|----------|
| T-003 | 实现登录逻辑 | 依赖外部 API | 2h 前 |
| T-004 | 调用登录 API | 依赖 T-003 | 2h 前 |

### 解除命令
- `/unblock T-003` - 解除 T-003
- `/unblock T-003 --cascade` - 级联解除
```

## 相关命令

- `/skip T-xxx` - 跳过 Ticket
- `/status` - 查看状态
