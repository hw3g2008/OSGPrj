# Local-only remote deploy settings (DO NOT commit real values)
# Copy:
#   cp deploy/.env.remote.local.example deploy/.env.remote.local

REMOTE_HOST=YOUR_TEST_SERVER_HOST
REMOTE_USER=root
REMOTE_DIR=/opt/OSGPrj

# Optional:
# SSH_KEY=~/.ssh/id_rsa
